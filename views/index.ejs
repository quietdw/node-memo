<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/css/style.css' />
  </head>
  <body>
    <div id="header">


      <ul>
        <% if (isLogin){ %>
        <li><img src="" alt=""></li>
        <li><span><%= user.username %></span></li>
        <li><span class="line"> | </span></li>
        <li><a class="logout" href="#">注销</a></li>
        <% }else{%>
        <li>
          <a href="">GitHub 登陆</a>
        </li>
        <% } %>
      </ul>
      <a href="#" title="添加笔记">+</a>
      <% if (isLogin){ %>
      <div class="tips">
        <h2>Good Morning <%= user.username %>!</h2>
        <p>You have <span>4 notes</span> to complete</p>
      </div>
      <% } %>
    </div>

    <div id="content">

      <div class="note">
        <span class="dot"></span>
        <div class="note=content">
          Don't forget the paln for a trip
        </div>
      </div>
      <div class="note">
        <span class="dot"></span>
        <div class="note=content">
          Don't forget the paln for a xxx
        </div>
      </div>
      <div class="note">
        <span class="dot"></span>
        <div class="note=content">
          Don't forget the paln for a yyy
        </div>
      </div>
      <div class="note">
        <span class="dot"></span>
        <div class="note=content">
          Don't forget the paln for a ddd
        </div>
      </div>
    </div>
    <div id="background"></div>
  </body>
</html>
